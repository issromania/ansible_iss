#!/usr/bin/env bash

(
echo "host port id path drain progress files bytes-left timeleft failed"
eos fs ls -b --drain | tail -n +4 | head -n -2 | sed -r 's/.spacescience.ro//g; s/ K/K/g; s/ M/M/g; s/ G/G/g ; s/ T/T/g; s/\s+/ /g'
) | column -t

