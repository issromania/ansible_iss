#!/usr/bin/env bash

(
echo "hostport id n(mem) n(disk) n(mgm) e(orph) e(unreg) e(layout) e(miss) e(disksize) e(mgmsize) e(disk-cx) e(mgm-cx)"
eos fs ls -b --fsck | tail -n +4 | head -n -2 | sed -r 's/.spacescience.ro//g; s/ K/K/g; s/ M/M/g; s/ G/G/g ; s/ T/T/g; s/\s+/ /g'
) | column -t

